APP_ENV=production
DB_HOST=localhost
DB_NAME=sala
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Lista de IPs/redes permitidas para ingresar al sistema, separadas por coma.
# Admite IPs sueltas o rangos CIDR (ej: 190.123.45.67,192.168.1.0/24).
# Si se deja vacío, no se restringe el acceso por IP.
ALLOWED_IPS=
